{
  // Excepciones documentadas — ver tasks/todo.md ("SEGUIMIENTO — npm audit")
  // y tasks/lessons.md (2026-07-23) para el razonamiento completo. Todas
  // confinadas a tooling de build (CLI de Sanity, routing-utils del adapter
  // de Vercel), no al runtime que sirve al público. Revisar en cada
  // actualización de `sanity` / `@astrojs/vercel` si upstream ya las parchó.
  "high": true,
  "critical": true,
  "allowlist": [
    "GHSA-xcpc-8h2w-3j85", // adm-zip vía @sanity/cli (tooling de build de Sanity, no runtime)
    "GHSA-mh29-5h37-fv8m", // js-yaml vía @vercel/frameworks (tooling de build)
    "GHSA-h67p-54hq-rp68", // js-yaml vía @vercel/frameworks (tooling de build)
    "GHSA-52cp-r559-cp3m", // js-yaml vía @vercel/frameworks (tooling de build)
    "GHSA-9wv6-86v2-598j", // path-to-regexp vía @vercel/routing-utils (solo genera routing en build, no en runtime)
    "GHSA-w5hq-g745-h8pq", // uuid vía typeid-js/@sanity/cli-core (tooling de build de Sanity, no runtime)
    "GHSA-ph9p-34f9-6g65", // tmp vía inquirer@6 (@lhci/cli) — solo corre en CI, prefijos fijos, no input de usuario
  ],
}
